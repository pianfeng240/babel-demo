
const cat = 'May';

const arrow = () => `Name: ${cat}`.padStart(2);

let map = new Map();

class Cat {
  constructor(name){
    this.name= name
  }
  getName(){
    return
  }
}

const a = [1,2].includes(2)

var promise = new Promise();
promise.resolve('ok')

async function asycFunc() {
  await promise();
}
