
const cat = 'May';

const arrow = () => `Name: ${cat}`.padStart(2);

let map = new Map();

class Cat {
  constructor(name){
    this.name= name
  }
  getName(){
    return
  }
}

var promise = new Promise();
promise.resolve('ok')

async function asycFunc() {
  await promise();
}
